const city: string = 'dhaka';
const fruits: string[] = ['mango', 'banana', 'apple'];

type person = { name: string, age: NamedCurve, salary: number }

const student: { name: string, age: number, salary: number } = {
    name: 'rayhan',
    age: 11500,
    salary: 5000
}